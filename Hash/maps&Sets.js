const a = new Map();

// with maps we can save any data type as keys.
// (functions of arrays also)
// with objects, that's not possible
// Map() maintains insertion order. So that we can
// loop through

const b = new Set();

// Sets only stores the keys. No values.